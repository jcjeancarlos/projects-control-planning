

function ProjectDescription() {
return (  
  <div>
     <textarea className='ProjectDescription' placeholder="Project description..." maxLength={300}/>
  </div>
)
};

export default ProjectDescription;